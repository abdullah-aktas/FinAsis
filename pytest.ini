[pytest]
# Single source of truth for tests
DJANGO_SETTINGS_MODULE = src.config.settings
# Add nested project directory so 'src' is importable
pythonpath = FinAsis
django_find_project = true
python_files = tests.py test_*.py *_tests.py
addopts = -q --maxfail=1
testpaths = tests